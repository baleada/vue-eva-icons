const presets = [
        [
          '@babel/preset-env',
          {
            targets: '> 0.5%, not dead',
            modules: false,
          },
        ],
      ],
      plugins = [

      ]

module.exports = { presets, plugins }
